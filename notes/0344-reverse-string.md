# 0344. Reverse String

## Idea

Use two pointers: one at the start and one at the end. Swap both characters, then move both pointers toward the center.

## Complexity

- Time: `O(n)`
- Space: `O(1)`

